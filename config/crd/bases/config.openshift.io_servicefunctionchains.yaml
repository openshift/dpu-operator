---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: servicefunctionchains.config.openshift.io
spec:
  group: config.openshift.io
  names:
    kind: ServiceFunctionChain
    listKind: ServiceFunctionChainList
    plural: servicefunctionchains
    shortNames:
    - sfc
    singular: servicefunctionchain
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: ServiceFunctionChain is the Schema for the servicefunctionchains
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ServiceFunctionChainSpec defines the desired state of ServiceFunctionChain
            properties:
              networkFunctions:
                items:
                  properties:
                    chart:
                      description: Chart defines a Helm chart for complex vendor deployments
                      properties:
                        name:
                          description: Name of the requested chart
                          type: string
                        repository:
                          description: Repository URL where to locate the requested
                            chart
                          type: string
                        version:
                          description: Version of the chart
                          type: string
                      required:
                      - name
                      - repository
                      - version
                      type: object
                    image:
                      description: Image is the raw container image (legacy/simple
                        deployment)
                      type: string
                    name:
                      type: string
                  required:
                  - name
                  type: object
                  x-kubernetes-validations:
                  - message: exactly one of image or chart must be set
                    rule: has(self.image) != has(self.chart)
                type: array
              nodeSelector:
                additionalProperties:
                  type: string
                description: |-
                  NodeSelector specifies which nodes this ServiceFunctionChain CR should be able to create the Network Function pod.
                  If empty, the ServiceFunctionChain will try to deploy the Network Function pod on all nodes.
                type: object
            required:
            - networkFunctions
            type: object
          status:
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
