---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: dataprocessingunits.config.openshift.io
spec:
  group: config.openshift.io
  names:
    kind: DataProcessingUnit
    listKind: DataProcessingUnitList
    plural: dataprocessingunits
    singular: dataprocessingunit
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.dpuProductName
      name: DPU Product
      type: string
    - jsonPath: .spec.isDpuSide
      name: DPU Side
      type: boolean
    - jsonPath: .status.status
      name: Status
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: DataProcessingUnit is the Schema for the dataprocessingunits
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DataProcessingUnitSpec defines the desired state of DataProcessingUnit
            properties:
              dpuProductName:
                description: DpuProductName is the vendor and model name of the DPU
                type: string
              isDpuSide:
                description: IsDpuSide indicates if this DPU is on the DPU side
                type: boolean
            required:
            - isDpuSide
            type: object
          status:
            description: DataProcessingUnitStatus defines the observed state of DataProcessingUnit
            properties:
              status:
                description: Status is the status of the DPU
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
